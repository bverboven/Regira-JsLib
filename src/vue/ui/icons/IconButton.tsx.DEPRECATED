// import type { Slots, VNode } from "vue"

// type IconIn = { icon: string; type?: "button" | "submit" | "reset" }

// export function IconButton({ icon, type = "button" }: IconIn, { slots }: { slots: Slots }): VNode {
//     return (
//         <button type={type} class={["btn"]}>
//             <IconButton.Icon name={icon} />
//             {slots.default && slots.default()}
//         </button>
//     )
// }
// IconButton.Icon = {} as any

// export default IconButton
